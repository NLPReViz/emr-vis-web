{
  "name": "emr-vis-web",
  "private": true,
  "version": "0.0.0",
  "description": "Web port of emr-vis-nlp",
  "repository": "https://github.com/trivedigaurav/emr-vis-web",
  "devDependencies": {
    "bower": "^1.3.1",
    "grunt": "~0.4.5",
    "grunt-replace": "^0.7.8"
  },
  "config": {
    "backend": "/emr-nlp-server",
    "appFolder": "app"
  },
  "scripts": {
    "postinstall": "bower install; grunt",
    "prestart": "bower install; grunt",
    "start": "echo 'App ready for deployment!'"
  }
}
