{
  "name": "emr-vis-web",
  "private": true,
  "version": "0.0.0",
  "description": "Web port of emr-vis-nlp",
  "repository": "https://github.com/trivedigaurav/emr-vis-web",
  "devDependencies": {
    "bower": "^1.3.1",
    "grunt": "~0.4.5",
    "grunt-cli": "~0.1.13",
    "grunt-replace": "^0.7.8"
  },
  "config": {
    "backend": "/emr-nlp-server",
    "appFolder": "app"
  },
  "scripts": {
    "postinstall": "bower install && npm start",
    "start": "grunt",
    "poststart": "echo App ready for deployment! If the project directory is not inside Tomcat webapps, you may move it now.",
    "stop": "echo This does not do anything now. Use catalina to stop the webserver."
  }
}
